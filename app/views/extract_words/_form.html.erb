<%= form_for(@extract_word) do |f| %>
  <% if @extract_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@extract_word.errors.count, "error") %> prohibited this extract_word from being saved:</h2>

      <ul>
      <% @extract_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit', :class => 'btn' %>
  </div>
<% end %>
